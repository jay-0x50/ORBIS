namespace Orbis.M15
{
    public enum CurrencyType { Lumen, OrbitShard, LimitedPledge, StandardPledge, AscensionStone, CodexMaterial }
    public enum CharacterRarity { Three = 3, Four = 4, Five = 5 }
    public enum WeaponType { Longsword, Bow, DualBlades, Greatsword, Spear, Catalyst }
    public enum BannerType { Limited, Standard }
}
